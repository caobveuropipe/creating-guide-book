interface:
  display_name: "Docs Hygiene"
  short_description: "Audit stale or orphaned .agents docs"
  default_prompt: "Use $docs-hygiene to audit the .agents documentation graph and propose cleanup actions."

policy:
  allow_implicit_invocation: true
